import { RentalPricePipe } from './rental-price.pipe';

describe('RentalPricePipe', () => {
  it('create an instance', () => {
    const pipe = new RentalPricePipe();
    expect(pipe).toBeTruthy();
  });
});
